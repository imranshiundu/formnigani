# Supabase (temporary hosted backend — see fng.txt §11 for the self-hosted target)
NEXT_PUBLIC_SUPABASE_URL=https://xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
# server-only: seeding + admin (NEVER prefix with NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# data provider: supabase when URL is set, otherwise local memory API
# (leave unset for offline frontend work)
NEXT_PUBLIC_DB=

# throwaway passwords for seeded test users (change after first login)
TEST_PASSWORD=FormTest123!
